## **Ejecucion del proyecto**

Es necesario tener instalado docker para ejectutar el contenedor.

No es necesario instalar ninguna otra tecnologia ya que al ejecutar los contenedores se instalan todas las dependencias necesarias para ejecutar el proyecto

Ejecutar contenedores
Ejecutar a la raiz del proyecto

```sh
docker compose up --build -d
```
